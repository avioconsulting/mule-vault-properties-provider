<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:vault-properties-provider="http://www.mulesoft.org/schema/mule/vault-properties-provider"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
          http://www.mulesoft.org/schema/mule/vault-properties-provider http://www.mulesoft.org/schema/mule/vault-properties-provider/current/mule-vault-properties-provider.xsd">

    <!--
    <vault-properties-provider:config name="config" vaultUrl="http://localhost:8200">
        <vault-properties-provider:basic vaultToken="s.uo18rIGCFexkcxOOJET97EPA" />
    </vault-properties-provider:config>

    <object name="testObject" class="com.avioconsulting.mule.vault.provider.TestObject">
        <property key="valueFromProperty" value="${vault-properties-provider::secret/test/mysecret.att1}" />
    </object>
    -->
    <vault-properties-provider:config name="config" vaultUrl="${vaultUrl}">
        <vault-properties-provider:basic vaultToken="${vaultToken}" kvVersion="1"/>
        <vault-properties-provider:ssl vaultPemFile="ssl/root-cert.pem" />
    </vault-properties-provider:config>

    <flow name="testFlow">
        <set-payload value="${vault-properties-provider::secret/test/mysecret.att1}" />
    </flow>

</mule>
